import { ListViewContainer } from "components/libs";

export const HomeSection = () => (
  <section className="home-section__container">
    <div className="align-middle d-flex gap-3">
      <ListViewContainer />
    </div>
  </section>
);
